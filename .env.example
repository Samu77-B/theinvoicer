# Copy to .env for local development (never commit .env)

SECRET_KEY=generate-a-long-random-string
FLASK_DEBUG=true

# Optional: put SQLite + invoice files on a persistent path (e.g. Railway volume mount)
# DATA_DIR=/data

# Resend (https://resend.com) — use a verified domain in production
RESEND_API_KEY=re_xxxxxxxx
RESEND_FROM_EMAIL=Invoices <onboarding@resend.dev>
